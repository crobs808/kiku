{
  "name": "kiku-desktop-shell",
  "private": true,
  "version": "0.1.4",
  "scripts": {
    "icon:sync": "tauri icon ../../assets/kiku-app-logo-aqua.png",
    "dev": "pnpm run icon:sync && cargo clean -p kiku-desktop && tauri dev",
    "build": "tauri build --bundles app,dmg"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0"
  }
}
