{
  "name": "kiku-desktop-shell",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "cargo clean -p kiku-desktop && tauri dev",
    "build": "tauri build --bundles app,dmg"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0"
  }
}
