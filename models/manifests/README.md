# Model Manifests

This directory will hold versioned local model manifest files.

MVP expectation:

- Whisper-family model metadata
- local install paths
- checksum/integrity fields
- preset mapping (`best_accuracy`, `balanced`, `experimental_japanese`)
